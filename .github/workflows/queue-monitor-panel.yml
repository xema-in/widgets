on: 
    workflow_dispatch:

jobs:
  changed:
    runs-on: ubuntu-latest
    steps:
      - uses: dorny/paths-filter@v2.2.1
        id: filter
        with:
          filters: |
            ${{GITHUB_WORKFLOW}}:
              - 'projects/${{GITHUB_WORKFLOW}}/package.json'

  publish:
    if: ${{ changed.steps.filter.${{GITHUB_WORKFLOW}} == 'true' }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
